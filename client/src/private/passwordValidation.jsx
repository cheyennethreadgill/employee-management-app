export const handlePasswordValidation = (e) => {
  // if password is too long
  if (e.length > 40) {
    console.log("password too long");
  } else return;
};
// export const handlef = (e) => {
//   // if password is too long
//   if (e.length > 40) {
//     console.log("password too long");
//   } else console.log(` Input ${e}`);
// };
